import styled from 'styled-components';

export const Container = styled.div`
  color: #f8ffff;
  font-weight: 800;
  letter-spacing: 1px;

  ul {
    list-style: none;
    display: flex;
  }

  ul li {
    margin-left: 16px;
  }
`;
